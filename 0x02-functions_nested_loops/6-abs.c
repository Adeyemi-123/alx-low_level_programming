#include "main.h"

/**
 * _abs - absolute numbers
 * @k: the integer we are working on
 * Return: return the absolute values of k and -k
 */

int _abs(int k)
{
	if (k > 0)
		return (k);
	else
		return (-k);
}
